import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-music-social',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
